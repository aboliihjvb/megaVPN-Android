name = "megavpn-admin"
main = "worker.js"
compatibility_date = "2026-09-08"

# Create a D1 database, then replace the placeholder below with its ID.
[[d1_databases]]
binding = "DB"
database_name = "megavpn"
database_id = "REPLACE_WITH_D1_DATABASE_ID"
